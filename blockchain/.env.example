# Anchor reads ANCHOR_PROVIDER_URL and ANCHOR_WALLET from env when --provider flags are absent.
# Copy to .env (loaded by direnv / dotenv) — never commit .env.

ANCHOR_PROVIDER_URL=https://api.devnet.solana.com
ANCHOR_WALLET=~/.config/solana/devnet-deployer.json
